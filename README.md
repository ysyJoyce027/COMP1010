#COMP 1010 编程作业

##A1 一个月前的事情了，不太记得自己学到了啥哈哈。
-Q2：十进制转换二进制和十六进制并验证
  **运用运算符号（//、%、**）；意识到字符和数字的转换
  *可优化：1.运用ASCII表，加个固定值，就不用那么繁琐地写一大堆字母数字转换了；
          2.缺少验证二进制和十六进制转会的值是否等于输入值，而是用了链式转换
-Q3：templist初始为[0,1],第i项的值为(i-2)+(i-1),最终输出templist长度为N（输入的正数）
  **学会了读取从列表的最后开始读取

##A2
-Q1Part1：输入一个数（不多于四位）。假如不到四位，前面用0补成四位数。
         要求最终的四位数包含至少两个不同的数字。将这个四位数中的数字按照升序和降序排列，组成成升序数和降序数。
         用降序数减去升序数，得到新数。如果新数不等于原来的四位数，则对这个新数进行同样的程序，直到新数等于旧数。
         计算每一次得到的新数是什么，并输出最终的循环次数以及最终数字的第一位和第三位。
  **尝试定义新函数；list(N)可以把N变为列表；.remove从列表中移除元素、.join把列表中每一个元素组合在一起、.append把元素加载列表最后
  -**定义validate_input(四位数包含两个不同的数字),sort_digit(n,b)(b=0/1时生成升序/降序数）
-Q1Part2：1-9998中所有整数，最多需要循环多少次才能使新数=旧数
-Q2：有一个4*4的棋盘，需要放置四个皇后，皇后会击杀在同一水平线、竖直线、对角线上的其他皇后。求四个皇后要怎样摆放才能同时存活。
  **矩阵的使用（list of list），矩阵中元素的表示[row][col]，for循环嵌套，top-to-bottom逻辑进行编写（不断产生新函数），return -1（标志着失败/无效），定义新变量以免对原变量造成影响
-Q3：多个vertices组成一个图形，图形中有一定数量的三边三角形。编写两个函数，函数1解释矩阵的乘法规则，函数2计算矩阵中主对角线上的和。比较（主对角线的和的三次方）除以6和图形中三边三角形个数的关系。
  **更多层的for嵌套，矩阵的乘法规则在python里的表达，矩阵中元素的表达新认知（[row][col]只是表达顺序，具体怎样表示row和col是自由的，比如B[colA][colB],A[i][i])
